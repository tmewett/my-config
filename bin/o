#!/usr/bin/env fish
function +
    set -ga choices $argv
end

+ 'open $argv'
+ 'w vscodium $argv'

if not set -q choices[1]; exit 1; end
set this (status basename)
if test $this = oi
    set command (printf "%s\0" $choices | fzf --read0)
    and eval $command
else
    eval $choices[1]
end
